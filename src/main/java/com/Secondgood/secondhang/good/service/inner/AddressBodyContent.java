package com.Secondgood.secondhang.good.service.inner;

import java.util.List;
import java.util.Objects;

public class AddressBodyContent {
/*
    private String name;

    private String password;

    private List<AddressContent> address;

    public AddressBodyContent(String name, String password, List<AddressContent> address) {
        this.name = name;
        this.password = password;
        this.address = address;
    }

    public AddressBodyContent() {
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (!(o instanceof AddressBodyContent)) return false;
        AddressBodyContent that = (AddressBodyContent) o;
        return Objects.equals(name, that.name) &&
                Objects.equals(password, that.password) &&
                Objects.equals(address, that.address);
    }

    @Override
    public String toString() {
        return "AddressBodyContent{" +
                "name='" + name + '\'' +
                ", password='" + password + '\'' +
                ", address=" + address +
                '}';
    }

    @Override
    public int hashCode() {
        return Objects.hash(name, password, address);
    }

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    public String getPassword() {
        return password;
    }

    public void setPassword(String password) {
        this.password = password;
    }

    public List<AddressContent> getAddress() {
        return address;
    }

    public void setAddress(List<AddressContent> address) {
        this.address = address;
    }

 */
}
